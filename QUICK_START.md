# دليل البدء السريع - Quick Start Guide

## 🚀 خطوات البدء السريع

### 1. تثبيت المتطلبات
```bash
npm install
cd client && npm install && cd ..
```

### 2. إعداد قاعدة البيانات
```bash
# أنشئ ملف .env في المجلد الرئيسي
# وأضف:
DATABASE_URL=postgresql://user:password@localhost:5432/sttlment
NODE_ENV=development
PORT=5001
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=7d
TIMEZONE=Asia/Baghdad
```

### 3. تشغيل الهجرات والبيانات التجريبية
```bash
# تشغيل الهجرات (إنشاء الجداول)
npm run migrate

# إضافة البيانات التجريبية (مستخدمين، فئات، قوالب، جداول)
npm run seed
```

### 4. تشغيل التطبيق
```bash
npm run dev
```

سيتم فتح التطبيق على:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:5001

---

## 🎯 تسجيل الدخول السريع

### الطريقة الأسهل: صفحة تسجيل الدخول السريع
افتح: **http://localhost:3000/quick-login**

ستجد أزرار لكل نوع مستخدم - اضغط على الزر للدخول مباشرة!

### بيانات تسجيل الدخول
**كلمة المرور لجميع الحسابات: `123456`**

| الدور | البريد الإلكتروني |
|------|------------------|
| 👑 مدير | `admin@alsaqi.com` |
| 👨‍💼 مشرف | `supervisor@alsaqi.com` |
| 👤 موظف 1 | `employee1@alsaqi.com` |
| 👤 موظف 2 | `employee2@alsaqi.com` |
| 👤 موظف 3 | `employee3@alsaqi.com` |
| 👤 موظف 4 | `employee4@alsaqi.com` |

---

## 📋 البيانات التجريبية المضافة

### ✅ تم إضافة:
- **6 مستخدمين** (مدير، مشرف، 4 موظفين)
- **7 فئات** (تسويات البنوك، مطابقة التحصيلات، إلخ)
- **8 قوالب مهام** (تسويات مصرف الرشيد، عمولة صندوق سهداء، إلخ)
- **5 جداول زمنية**:
  - يومية: تسويات مصرف الرشيد (الاثنين-الجمعة)
  - يومية: تسويات البنك الأهلي (الاثنين-الجمعة)
  - أسبوعية: تقرير الأداء الأسبوعي (كل أحد)
  - شهرية: عمولة صندوق سهداء الشرطة (يوم 1 من كل شهر)
  - يومية: مطابقة تحصيلات الكهرباء (الاثنين-الجمعة)
- **3 مهام خاصة** (Ad-hoc)
- **حضور لليوم** لجميع المستخدمين

---

## 🧪 اختبار النظام

### 1. كمدير
1. سجل دخول: `admin@alsaqi.com` / `123456`
2. جرب:
   - إدارة المستخدمين
   - عرض التقارير
   - إعدادات TV
   - سجل التدقيق

### 2. كمشرف
1. سجل دخول: `supervisor@alsaqi.com` / `123456`
2. جرب:
   - إدارة الفئات
   - إدارة القوالب
   - إدارة الجداول (أنشئ جدول يومي/أسبوعي/شهري)
   - عرض التقارير

### 3. كموظف
1. سجل دخول: `employee1@alsaqi.com` / `123456`
2. جرب:
   - عرض المهام اليومية
   - تنفيذ مهمة (أضف ملاحظات ومرفقات)
   - إنشاء مهمة خاصة (إذا كان لديه الصلاحية)
   - البحث والفلترة

---

## 🔄 إعادة تعيين البيانات

لإعادة تعيين البيانات التجريبية:

```bash
npm run seed
```

> ⚠️ **ملاحظة**: `seed` يستخدم `ON CONFLICT DO NOTHING` أو `ON CONFLICT DO UPDATE`، لذا لن يحذف البيانات الموجودة، بل سيحدثها فقط.

---

## 📖 المزيد من المعلومات

- [`SYSTEM_OVERVIEW.md`](./SYSTEM_OVERVIEW.md) - نظرة شاملة على النظام
- [`TEST_DATA.md`](./TEST_DATA.md) - تفاصيل بيانات الاختبار
- [`SETUP.md`](./SETUP.md) - دليل الإعداد الكامل
- [`README.md`](./README.md) - نظرة عامة على المشروع

---

## ❓ حل المشاكل الشائعة

### المشكلة: "DATABASE_URL غير موجود"
**الحل**: تأكد من وجود ملف `.env` في المجلد الرئيسي مع `DATABASE_URL` صحيح.

### المشكلة: "Port 5000 already in use"
**الحل**: تم تغيير المنفذ الافتراضي إلى 5001. تأكد من أن `.env` يحتوي على `PORT=5001`.

### المشكلة: "Cannot connect to database"
**الحل**: 
1. تأكد من أن PostgreSQL يعمل
2. تحقق من `DATABASE_URL` في `.env`
3. جرب الاتصال يدوياً: `psql DATABASE_URL`

---

## 🎉 جاهز للاختبار!

افتح **http://localhost:3000/quick-login** وابدأ الاختبار! 🚀

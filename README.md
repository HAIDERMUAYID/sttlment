# نظام إدارة المهام والحضور
## قسم التسويات والمطابقة - شركة الصاقي للدفع الإلكتروني

### نظرة عامة
نظام شامل لإدارة المهام المجدولة والخاصة، تتبع الحضور، والتقارير التحليلية مع لوحة تحكم تلفزيونية احترافية.

**أنواع المهام المدعومة:**
- **يومية**: أيام أسبوع محددة (مثل: تسويات مصرف الرشيد - كل سبت وأحد...)
- **أسبوعية**: يوم واحد من الأسبوع (مثل: تقرير أسبوعي - كل يوم أحد)
- **شهرية**: يوم محدد من الشهر (مثل: عمولة صندوق سهداء الشرطة - يوم 1 من كل شهر)
- **غير مرتبطة بوقت**: مهام خاصة (مثل: تقارير حركات، إعداد كتب رسمية)

> 📖 للمزيد من التفاصيل، راجع [`SYSTEM_OVERVIEW.md`](./SYSTEM_OVERVIEW.md)  
> 🧪 لبيانات الاختبار وتسجيل الدخول السريع، راجع [`TEST_DATA.md`](./TEST_DATA.md)

---

## 🚀 دليل النشر على Render

### المتطلبات الأساسية
- حساب Render
- قاعدة بيانات PostgreSQL (Render Postgres أو Supabase)
- مستودع GitHub

### خطوات النشر

#### 1. إعداد قاعدة البيانات
1. أنشئ قاعدة بيانات PostgreSQL على Render
2. احفظ `DATABASE_URL` من إعدادات قاعدة البيانات

#### 2. إعداد متغيرات البيئة على Render
في إعدادات الخدمة (Web Service)، أضف المتغيرات التالية:

```
NODE_ENV=production
PORT=10000
DATABASE_URL=<your-database-url>
JWT_SECRET=<generate-a-strong-secret-key>
JWT_EXPIRES_IN=7d
TIMEZONE=Asia/Baghdad
```

#### 3. إعداد الخدمة الرئيسية (Web Service)
- **Build Command**: `npm install && npm run build`
- **Start Command**: `npm start`
- **Environment**: Node
- **Auto-Deploy**: Yes (عند الدفع إلى main branch)

#### 4. إعداد Cron Jobs على Render

##### أ) مهمة توليد المهام اليومية
- **Schedule**: `0 1 * * *` (كل يوم الساعة 1:00 صباحاً UTC = 4:00 صباحاً بتوقيت بغداد)
- **Command**: `node server/cron/generateDailyTasks.js`
- **Environment Variables**: نفس متغيرات الخدمة الرئيسية

##### ب) مهمة فحص المهام المتأخرة
- **Schedule**: `*/15 * * * *` (كل 15 دقيقة)
- **Command**: `node server/cron/checkOverdueTasks.js`
- **Environment Variables**: نفس متغيرات الخدمة الرئيسية

#### 5. تشغيل الهجرات والبيانات الأولية
بعد النشر الأول، قم بتشغيل:
```bash
npm run migrate
npm run seed
```

أو قم بذلك يدوياً عبر Render Shell.

---

## 📁 هيكل المشروع

```
sttlment/
├── server/
│   ├── index.js              # نقطة البداية
│   ├── config/
│   │   └── database.js       # إعدادات قاعدة البيانات
│   ├── models/               # نماذج البيانات
│   ├── routes/               # مسارات API
│   ├── middleware/           # Middleware (auth, validation)
│   ├── controllers/          # منطق الأعمال
│   ├── migrations/           # هجرات قاعدة البيانات
│   ├── cron/                 # مهام Cron
│   └── utils/                # أدوات مساعدة
├── client/
│   ├── src/
│   │   ├── components/      # مكونات React
│   │   ├── pages/           # الصفحات
│   │   ├── services/        # خدمات API
│   │   └── utils/           # أدوات
│   └── public/
└── package.json
```

---

## 🔐 الأدوار والصلاحيات

- **Admin (مدير القسم)**: إدارة كاملة للمستخدمين، التقارير، إعدادات لوحة التحكم
- **Supervisor (مساعد المدير)**: إدارة الكتالوج، الجداول، المهام الخاصة
- **Employee (موظف)**: تنفيذ المهام، إنشاء مهام خاصة (إذا مسموح)
- **Viewer (عارض)**: عرض لوحة التحكم فقط

---

## 🗄️ قاعدة البيانات

يتم استخدام PostgreSQL مع الهجرات التلقائية. الجداول الرئيسية:
- Users, Categories, TaskTemplates, Schedules
- DailyTasks, TaskExecutions, AdHocTasks
- Attendance, AuditLog, Settings

---

## 📊 الميزات الرئيسية

1. **كتالوج المهام**: فئات وقوالب قابلة للتخصيص
2. **المهام المجدولة**: توليد تلقائي يومي من الجداول
3. **تتبع الحضور**: تسجيل أول تسجيل دخول يومي
4. **التقارير**: يومية، أسبوعية، شهرية مع تصدير Excel/PDF
5. **لوحة التحكم التلفزيونية**: عرض شرائح احترافي على شاشة 86 بوصة

---

## 🛠️ التطوير المحلي

```bash
# تثبيت التبعيات
npm install
cd client && npm install && cd ..

# إعداد .env
cp .env.example .env
# عدّل .env بإعدادات قاعدة البيانات المحلية

# تشغيل الهجرات
npm run migrate
npm run seed

# تشغيل التطوير
npm run dev
```

---

## 📝 ملاحظات

- جميع الأوقات تُحفظ في UTC وتُعرض بتوقيت بغداد
- المهام المتأخرة تُفحص كل 15 دقيقة
- المهام اليومية تُولد تلقائياً كل يوم
- جميع الواجهات باللغة العربية

---

## 🔒 الأمان

### تحديث الحزم
بعد تثبيت التبعيات، يُنصح بتشغيل:
```bash
npm audit fix
```

للتعامل مع الثغرات الأمنية. بعض التحذيرات قد تكون من حزم التطوير (devDependencies) ولا تؤثر على الإنتاج.

### متغيرات البيئة الحساسة
- **JWT_SECRET**: استخدم مفتاحاً قوياً وعشوائياً في الإنتاج
- **DATABASE_URL**: لا تشارك رابط قاعدة البيانات أبداً
- استخدم متغيرات البيئة على Render ولا تضعها في الكود

---

## 📋 خطة التطوير

لتفاصيل التحسينات المقترحة (واجهة، تقارير، TV، أمان، اختبارات)، راجع **[DEVELOPMENT_ROADMAP.md](./DEVELOPMENT_ROADMAP.md)**.

---

## 📧 الدعم

للمساعدة التقنية، يرجى التواصل مع فريق التطوير.